<UserControl x:Class="ERHMS.Presentation.Views.IncidentNotesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignWidth="640" d:DesignHeight="480"
             Name="Root">
    <DockPanel>
        <TextBox DockPanel.Dock="Top" AcceptsReturn="True" TextWrapping="Wrap" Height="100"
                 Text="{Binding Note.Content, UpdateSourceTrigger=PropertyChanged}" />
        <WrapPanel DockPanel.Dock="Top" Style="{StaticResource AppBar}">
            <Button Command="{Binding SaveCommand}">Save</Button>
        </WrapPanel>
        <WrapPanel DockPanel.Dock="Bottom" Style="{StaticResource AppBar}">
            <Button Command="{Binding RefreshCommand}">Refresh</Button>
        </WrapPanel>
        <ListView ItemsSource="{Binding Notes}" Style="{StaticResource IncidentNotes}" />
    </DockPanel>
</UserControl>
