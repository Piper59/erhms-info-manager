<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                    xmlns:scw="clr-namespace:MahApps.Metro.SimpleChildWindow;assembly=MahApps.Metro.SimpleChildWindow"
                    xmlns:conv="clr-namespace:ERHMS.Presentation.Converters"
                    xmlns:ctrl="clr-namespace:ERHMS.Presentation.Controls">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.xaml" />
        <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Fonts.xaml" />
        <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Colors.xaml" />
        <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Accents/Blue.xaml" />
        <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Accents/BaseLight.xaml" />
        <ResourceDictionary Source="../Resources/Icons.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="{x:Type ctrl:AppBarButton}" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <StackPanel>
                        <Rectangle Width="25" Height="20" Fill="{DynamicResource BlackBrush}">
                            <Rectangle.OpacityMask>
                                <VisualBrush Stretch="Uniform"
                                             Visual="{Binding Visual, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ctrl:AppBarButton}}}" />
                            </Rectangle.OpacityMask>
                        </Rectangle>
                        <ContentPresenter HorizontalAlignment="Center" Margin="0" TextBlock.FontSize="12"
                                          Content="{Binding Content, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ctrl:AppBarButton}}}" />
                    </StackPanel>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Height" Value="50" />
        <Setter Property="Margin" Value="5,0,0,5" />
        <Setter Property="MinWidth" Value="50" />
    </Style>

    <Style TargetType="{x:Type scw:ChildWindow}">
        <Setter Property="ChildWindowWidth" Value="400" />
    </Style>

    <Style TargetType="{x:Type ctrl:FormGrid}">
        <Style.Resources>
            <Style TargetType="{x:Type Label}">
                <Setter Property="HorizontalAlignment" Value="Right" />
            </Style>
        </Style.Resources>
    </Style>

    <Style TargetType="{x:Type ListView}" x:Key="IncidentNotes">
        <Style.Resources>
            <conv:DateTimeToStringConverter x:Key="DateTimeToStringConverter" />
        </Style.Resources>
        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="ListViewItem" BasedOn="{StaticResource {x:Type ListViewItem}}">
                    <Setter Property="Focusable" Value="False" />
                </Style>
            </Setter.Value>
        </Setter>
        <Setter Property="ItemTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Border BorderBrush="LightGray" BorderThickness="0,1,0,0">
                        <StackPanel Margin="5">
                            <TextBlock TextWrapping="Wrap" Margin="0,0,0,5" Text="{Binding Content}" />
                            <TextBlock Foreground="Gray" Text="{Binding Date, Converter={StaticResource DateTimeToStringConverter}}" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
    </Style>

    <Style TargetType="{x:Type mah:MetroTabControl}">
        <Setter Property="TabStripMargin" Value="0,0,10,0" />
        <Setter Property="TabStripPlacement" Value="Left" />
    </Style>

    <Style TargetType="{x:Type mah:MetroTabItem}">
        <Setter Property="mah:ControlsHelper.HeaderFontSize" Value="18" />
    </Style>

    <Style TargetType="{x:Type ctrl:MultiSelectDataGrid}" BasedOn="{StaticResource {x:Type DataGrid}}" />

    <Style TargetType="{x:Type TextBlock}" x:Key="DocumentTitle">
        <Setter Property="FontSize" Value="18" />
        <Setter Property="Margin" Value="5,0,0,0" />
        <Setter Property="MaxWidth" Value="200" />
        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
    </Style>

    <Style TargetType="{x:Type TextBlock}" x:Key="Numeric">
        <Setter Property="HorizontalAlignment" Value="Right" />
    </Style>

    <Style TargetType="{x:Type ScrollViewer}">
        <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
    </Style>

    <Style TargetType="{x:Type WrapPanel}" x:Key="AppBar">
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="Margin" Value="0,5,10,0" />
    </Style>
</ResourceDictionary>
